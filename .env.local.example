# Local Next.js env (copy to .env.local if you want defaults on the server side).
# For the SPA login flow, UCI + password are entered in the browser and posted
# to /api/ircc/* — server env vars are optional fallbacks only.

# Optional Cognito / API overrides
# COGNITO_URL=https://cognito-idp.ca-central-1.amazonaws.com/
# COGNITO_CLIENT_ID=3cfutv5ffd1i622g1tn6vton5r
# IRCC_API_URL=https://api.ircc-tracker-suivi.apps.cic.gc.ca/user
